/**
 * Automatic tracing (following a payment hop by hop) is parked for now: the engine and its
 * tests stay, but the buttons that start it are hidden. The `auto-trace` branch keeps it on.
 */
export const AUTO_TRACE = false
